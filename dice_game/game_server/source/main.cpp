#include <iostream>

int main(void)
{
	// Initialize the server

	// Update the server need to figure out a way to send a kill message that doesn't block

	// Shutdown server

	std::cout << "Thanks for playing!";
	system("pause");
	return 0;
}